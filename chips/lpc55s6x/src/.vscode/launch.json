{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (probe-rs DAP)",
      "type": "probe-rs-debug", // <-- THE KEY CHANGE: Use the native probe-rs debugger type
      "request": "launch",
      "cwd": "${workspaceRoot}",
      "connectUnderReset": false, // This is often a good default
      "chip": "LPC55S69JBD100", // Your chip
      //"probe": "VID:PID", // Your probe, or remove this line if you only have one
      "executable": "/home/pc2/Downloads/tock/target/thumbv8m.main-none-eabihf/debug/lpc55s69-evk", // CHANGE THIS
      "core": 0, // Specify which core to attach to (0 is default)
      "coreConfigs":[
        {
            "core": 0,
            "programBinary": "/home/pc2/Downloads/tock/target/thumbv8m.main-none-eabihf/debug/lpc55s69-evk"
        }
      ]

      // These are for RTT logging, you can enable them if you use `rtt-target`
      // "rtt": {
      //   "enabled": true
      // }
    }
  ]
}